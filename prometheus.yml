global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'harness'
    static_configs:
      - targets: ['host.docker.internal:8000']
    metrics_path: /metrics

  # mcp-server does not expose a /metrics endpoint — harness metrics cover all tool calls
