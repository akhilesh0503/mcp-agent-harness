# MCP
mcp>=1.0.0

# LLM providers
anthropic>=0.40.0

# FastAPI + server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Async DB + cache
asyncpg>=0.29.0
redis[asyncio]>=5.0.0

# HTTP client (for http_api_call tool + Ollama)
httpx>=0.27.0

# Pydantic + settings
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Observability
prometheus-client>=0.20.0

# Config
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Hashing (audit log input hash)
# stdlib hashlib — no install needed

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# Dev
httpx>=0.27.0
